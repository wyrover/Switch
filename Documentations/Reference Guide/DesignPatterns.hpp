/// @page CommonDesignPatternsPage Common Design Patterns
///
/// Design patterns are solutions to software design problems you find again and again in real-world application development. Patterns are about reusable designs and interactions of objects.
///
/// The 23 Gang of Four (GoF) patterns are generally considered the foundation for all other patterns. They are categorized in three groups: Creational, Structural, and Behavioral (for a complete list see below).
///
/// To give you a head start, the Pcf C++ source code for each pattern is provided in structural form. Structural code uses type names as defined in the pattern definition and UML diagrams.
///
/// @section CreationalPatternsSection Creational Patterns
/// * @ref AbstractFactoryPage        Creates an instance of several families of classes
/// * @ref BuilderPage                Separates object construction from its representation
/// * @ref FactoryMethodPage	        Creates an instance of several derived classes
/// * @ref PrototypePage              A fully initialized instance to be copied or cloned
/// * @ref SingletonPage              A class of which only a single instance can exist
///
/// @section StructuralPatternsSection Structural Patterns
/// * @ref AdapterPage                Match interfaces of different classes
/// * @ref BridgePage                 Separates an object’s interface from its implementation
/// * @ref CompositePage              A tree structure of simple and composite objects
/// * @ref DecoratorPage              Add responsibilities to objects dynamically
/// * @ref FacadePage                 A single class that represents an entire subsystem
/// * @ref FlyweightPage              A fine-grained instance used for efficient sharing
/// * @ref ProxyPage                  An object representing another object
///
/// @section BehavioralPatternsSection Behavioral Patterns
/// * @ref ChainOfResponsabilityPage  A way of passing a request between a chain of objects
/// * @ref CommandPage                Encapsulate a command request as an object
/// * @ref InterpreterPage            A way to include language elements in a program
/// * @ref IteratorPage               Sequentially access the elements of a collection
/// * @ref MediatorPage               Defines simplified communication between classes
/// * @ref MementoPage                Capture and restore an object's internal state
/// * @ref ObserverPage               A way of notifying change to a number of classes
/// * @ref StatePage                  Alter an object's behavior when its state changes
/// * @ref StrategyPage               Encapsulates an algorithm inside a class
/// * @ref TemplateMethodPage         Defer the exact steps of an algorithm to a subclass
/// * @ref VisitorPage                Defines a new operation to a class without change
///
/// @section ReferencesSection References
/// http://www.dofactory.com/Patterns/Patterns.aspx
///
/// @section SeeSection See also
/// Other Resources
/// * @ref FrameworkDesignGuidelinesPage
