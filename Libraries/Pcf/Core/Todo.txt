Mandatory :
- investigate ConvertUnittest, DISABLED_To ( Boolean / Byte / Char ) FromDateTime
- use explicit keyword

Optional :
- Verify commented lines in ConvertUnittest (Date problems)
- Handle "G" format for floating values, then uncomment // and EXPECT_EQ(Convert::ToString(valueF64),"1E-05");
- String : add and use ascii flag ? (for [] operator)
- gtest : new flags (+ / - / --fail_wait=true --fw)
- Parse and TryParse : Refactor code