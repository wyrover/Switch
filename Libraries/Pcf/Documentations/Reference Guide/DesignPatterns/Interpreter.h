/**
 * @page InterpreterPage Interpreter
 *
 * A way to include language elements in a program
 *
 * @section DefinitionSection Definition
 * Given a language, define a representation for its grammar along with an interpreter that uses the representation to interpret sentences in the language.
 *
 * @section UmlClassDiagramSection UML class diagram
 * @image html interpreter.gif
 *
 * @section SampleCodeSection Sample code
 * This structural code demonstrates the Interpreter patterns, which using a defined grammer, provides the interpreter that processes parsed statements.
 * @include Interpreter.cpp
 */
