/**
 * @page VisitorPage Visitor
 *
 * Defines a new operation to a class without change
 *
 * @section DefinitionSection Definition
 * Represent an operation to be performed on the elements of an object structure. Visitor lets you define a new operation without changing the classes of the elements on which it operates.
 *
 * @section UmlClassDiagramSection UML class diagram
 * @image html visitor.gif
 *
 * @section SampleCodeSection Sample code
 * TThis structural code demonstrates the Visitor pattern in which an object traverses an object structure and performs the same operation on each node in this structure. Different visitor objects define different operations.
 * @include Visitor.cpp
 */
